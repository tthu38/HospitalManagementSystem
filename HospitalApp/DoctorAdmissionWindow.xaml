<Window x:Class="HospitalApp.DoctorAdmissionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admissions"
        Height="520" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Manage Admissions"
                       FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid x:Name="dgAdmissions"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Height="350">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Patient" Binding="{Binding Patient.FullName}" Width="150"/>
                    <DataGridTextColumn Header="Room" Binding="{Binding Room.RoomNumber}" Width="100"/>
                    <DataGridTextColumn Header="Admission Date" Binding="{Binding AdmissionDate}" Width="120"/>
                    <DataGridTextColumn Header="Discharge Date" Binding="{Binding DischargeDate}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>

                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="New Admission"
                        Width="130" Height="34"
                        Style="{StaticResource PrimaryButton}"
                        Click="BtnNew_Click"/>
                <Button Content="Discharge"
                        Width="110" Height="34"
                        Margin="10,0,0,0"
                        Style="{StaticResource PrimaryButton}"
                        Click="BtnDischarge_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
