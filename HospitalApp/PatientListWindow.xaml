<UserControl x:Class="HospitalApp.PatientListWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Manage Patients"
                   FontSize="22"
                   FontWeight="Bold"
                   Foreground="{StaticResource DarkBlue}"
                   Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Button Content="Add Patient"
                    Width="120"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnAdd_Click"/>
            <Button Content="Edit Patient"
                    Width="130"
                    Margin="10,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnEdit_Click"/>
            <Button Content="Toggle Active"
                    Width="130"
                    Margin="10,0,0,0"
                    Style="{StaticResource PrimaryButton}"
                    Click="BtnToggle_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgPatients"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding PatientId}" Width="50"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="150"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="80"/>
                <DataGridTextColumn Header="DOB" Binding="{Binding Dob}" Width="100"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="200"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
